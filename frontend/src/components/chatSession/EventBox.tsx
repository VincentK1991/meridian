// Re-export from the new modular EventBox structure
import EventBox, { BaseEventBox, UserEventBox, ModelEventBox, PartsRenderer } from './EventBox/index';

export default EventBox;
export { BaseEventBox, UserEventBox, ModelEventBox, PartsRenderer };
