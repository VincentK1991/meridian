[project]
name = "backend"
version = "0.1.0"
description = ""
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
    "uvicorn[standard]>=0.34.3"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
